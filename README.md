## HTTP File Server Written in Java

Run Server.java in IDE and go to `[http://localhost:8799/](localhost:8799)` you will get an HTTP file server whose root directory is the **root named** directory here
